import { memo } from "react";

const Header = memo(function Header() {
  return (
    <header className=" text-center font-thin  text-3xl">
      NakreS NotesAPP
    </header>
  );
});

export default Header;
